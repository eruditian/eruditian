import React, { useEffect } from 'react';
import World from './maps/World';

const Geo: React.FC = () => {
  useEffect(() => {}, []);

  return (
    <div>
      <World />
    </div>
  );
};

export default Geo;
